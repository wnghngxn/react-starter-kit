import { routes } from './routes';
import { nav } from './nav';
import { USER } from './constances';
import { model } from './model';

const { name, path } = USER;

export { routes, name, path, nav, model };
